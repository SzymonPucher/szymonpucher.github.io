# Gemini Instructions

## Core Mandates
1.  **NEVER COMMIT OR PUSH:** You are strictly forbidden from running `git commit`, `git push`, or any other command that commits or pushes changes to the repository. The user handles all version control operations manually to ensure review of AI generated content before it's version contolled.
2.  **Project Context:** This project (`szymonpucher.github.io`) is the user's personal website, serving as their **CV, Portfolio, and Blog**.
3.  **Language:** The project is internationalized (English and Polish). Maintain this structure when adding new content.
